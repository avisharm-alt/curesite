{
  "buildCommand": "cd frontend && yarn install && yarn build",
  "outputDirectory": "frontend/build",
  "installCommand": "echo 'Skipping root install'",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet . ':(exclude)backend/'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}